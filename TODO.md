# TODO

- [ ] Add your CV PDF to the project as a static asset (copy into `public/`).
- [ ] Add a “View CV” link/button on the site (likely in `Navbar` and/or `Hero`/`Contact`).
- [ ] Ensure the link uses `/Chelsy_CV.pdf` (or the chosen filename) so it works in production.
- [ ] Run `npm run dev` and verify the link opens the PDF in the browser.

